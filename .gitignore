node_modules
.env
.env.local
.env.*.local
uploads
server/uploads/*
!server/uploads/.gitkeep
# Keep package-lock.json for consistent deployments
# dist/
# Uncomment dist/ if you want to commit built files
